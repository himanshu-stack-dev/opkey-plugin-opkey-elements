{% set defaults = {
  eyebrow: 'LOREM IPSUM DOLOR',
  heading: 'Lorem ipsum dolor sit amet',
  subhead: 'Lorem ipsum dolor sit amet consectetur adipiscing elit',
  buttons: {
    primary: { text: 'Contact sales', url: '#' },
    secondary: { text: 'Learn more', url: '#' }
  },
  quote: {
    text: 'Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat.',
    headshot: '/wp-content/plugins/opkey-elements/assets/Placeholder-headshot-big.png',
    name: 'Lorem Dolor',
    company: 'Lorem ipsum, Company',
    button: { text: 'Learn more', url: '#' }
  }
} %}

<div class="module-container">
  <div class="module-text">
    {# EYEBROW #}
    {% set eyebrow = content.eyebrow.text %}
    {% if eyebrow %}
      <div class="eyebrow">{{ eyebrow }}</div>
    {% endif %}

    {# HEADING #}
    {% set heading = content.heading.text  %}
    {% if heading %}
      <h2>{{ heading }}</h2>
    {% endif %}

    {# SUBHEAD #}
    {% set subhead = content.subhead.text  %}
    {% if subhead %}
      <h4>{{ subhead }}</h4>
    {% endif %}

    {# BUTTONS #}
    {% set pb = content.buttons.primary_button|default({}) %}
    {% set sb = content.buttons.secondary_button|default({}) %}

    {% if pb.text|default('')|trim or sb.text|default('')|trim %}
      <div class="content-module module-buttons">
        {% if pb.text|default('')|trim %}
          {{ macros.atomV1ButtonHtml(
               pb,
               'opkey-primary',
               path.to.button_design_preset_section,
               'primary',
               pb.text|trim
             ) }}
        {% endif %}
        {% if sb.text|default('')|trim %}
          {{ macros.atomV1ButtonHtml(
               sb,
               'opkey-link-text',
               path.to.button_design_preset_section,
               'secondary',
               sb.text|trim
             ) }}
        {% endif %}
      </div>
    {% endif %}
  </div>
  <div class="module-quote">
    <div class="quote-content">
      <div class="gradient-wrapper">
        <div class="breakdance-swiper-wrapper" data-swiper-id="%%ID%%">
          <div class="swiper swiper-%%UNIQUESLUG%%">
            <div class="swiper-wrapper">
              {% set slides = (content.quotes.slides is defined and content.quotes.slides|length > 0) 
                  ? content.quotes.slides 
                  : [defaults.quote] %}

              {% for item in slides %}
                {% set is_fallback = item.text == defaults.quote.text %}
                {% set btn_text = item.link_to_case_study.text|default('')|trim %}
                {% set btn_url = item.link_to_case_study.url|default('#')|trim %}

                <div class="swiper-slide">
                  <div class="bde-basicslider__slide">
                    <div>
                      <div class="bde-basicslider__content">
                        <div class="bde-basicslider__text">
                          {{ item.text }}
                          <div class="name-company">
                            <div class="name">{{ item.name }}</div>
                            <div class="company">{{ item.role_company }}</div>
                          </div>
                        </div>
                        <div class="quote-attribution">
                          <div class="headshot">
                            {% if item.headshot is defined and item.headshot is not empty 
                                  and (item.headshot.url is defined or item.headshot.ID is defined or item.headshot.id is defined) %}
                              {{ macros.image(item.headshot, content.content.size, alt, lazyLoad) }}
                            {% else %}
                              <img src="{{ defaults.quote.headshot }}" alt="Placeholder headshot" loading="lazy">
                            {% endif %}
                          </div>
                          <div class="name-company mobile">
                            <div class="name">{{ item.name }}</div>
                            <div class="company">{{ item.role_company }}</div>
                          </div>
                        </div>
                      </div>

                      {% if (btn_text and btn_url != '') or is_fallback %}
                        <div style="display:flex;">
                          {{ macros.atomV1ButtonHtml(
                            is_fallback 
                              ? defaults.quote.button 
                              : { text: btn_text, url: btn_url },
                            'bde-basicslider__button opkey-link-text',
                            design.button,
                            'content.quotes.slides[' ~ loop.index0 ~ '].link_to_case_study.text'
                          ) }}
                        </div>
                      {% endif %}
                    </div>
                  </div>
                </div>
              {% endfor %}
            </div>
          </div>
        </div>
      </div>
    </div>
    {% if slides|length > 1 %}
      <div class="slider-navigation">
        <button class="swiper-button-prev swiper-button-prev-%%UNIQUESLUG%% custom-nav-btn"></button>
        <div class="swiper-pagination swiper-pagination-%%UNIQUESLUG%%"></div>
        <button class="swiper-button-next swiper-button-next-%%UNIQUESLUG%% custom-nav-btn"></button>
      </div>
    {% endif %}
  </div>
</div>
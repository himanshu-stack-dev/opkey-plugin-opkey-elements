<div class="module-container">
  <div class="module-columns module-text">
    {# EYEBROW #}
    {% set eyebrow = content.eyebrow.text %}
    {% if eyebrow %}
      <div class="eyebrow">{{ eyebrow }}</div>
    {% endif %}

    {# HEADING #}
    {% set heading = content.heading.text  %}
    {% if heading %}
      <h2>{{ heading }}</h2>
    {% endif %}

    {# SUBHEAD #}
    {% set subhead = content.subhead.text  %}
    {% if subhead %}
      <h4>{{ subhead }}</h4>
    {% endif %}

    {# BODY #}
    {% set body = content.content.text  %}
    {% if body %}
      <p>{{ body }}</p>
    {% endif %}

    {# BUTTONS #}
    {% set pb = content.buttons.primary_button|default({}) %}
    {% set sb = content.buttons.secondary_button|default({}) %}

    {% if pb.text|default('')|trim or sb.text|default('')|trim %}
      <div class="content-module module-buttons">
        {% if pb.text|default('')|trim %}
          {{ macros.atomV1ButtonHtml(
               pb,
               'opkey-primary',
               path.to.button_design_preset_section,
               'primary',
               pb.text|trim
             ) }}
        {% endif %}
        {% if sb.text|default('')|trim %}
          {{ macros.atomV1ButtonHtml(
               sb,
               'opkey-link-text',
               path.to.button_design_preset_section,
               'secondary',
               sb.text|trim
             ) }}
        {% endif %}
      </div>
    {% endif %}
  </div>

  {# COLUMNS #}
  {% set default_columns = [
    {
      heading: "Lorem ipsum dolor sit amet consectetur adipiscing elit",
      content: "Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.",
      button: { text: "Learn more", url: "#" }
    },
    {
      heading: "Lorem ipsum dolor sit amet consectetur adipiscing elit",
      content: "Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.",
      button: { text: "Learn more", url: "#" }
    },
    {
      heading: "Lorem ipsum dolor sit amet consectetur adipiscing elit",
      content: "Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.",
      button: { text: "Learn more", url: "#" }
    }
  ] %}

  {% set has_quotes = content.columns is defined and content.columns.quotes is defined and content.columns.quotes|length > 0 %}
  {% set columns = has_quotes ? content.columns.quotes : default_columns %}

  <div class="columns">
    {% for item in columns %}
      <div class="quote-content">
        <div class="gradient-wrapper">
          <div class="column">
            <div class="column-container">
              <h4 class="bde-basicslider__title">
                {{ item.heading is defined and item.heading|trim|length > 0
                  ? item.heading
                  : 'Lorem ipsum dolor sit amet consectetur adipiscing elit' }}
              </h4>
              <p>
                {{ item.content is defined and item.content|trim|length > 0
                  ? item.content
                  : 'Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.' }}
              </p>
              <div class="quote-attribution">
                <div class="headshot">
                  {% if item.headshot is defined and item.headshot is not empty %}
                    {{ macros.image(item.headshot, content.content.size, alt, lazyLoad) }}
                  {% else %}
                    <img src="/wp-content/uploads/2025/07/Placeholder_headshot.png" alt="Placeholder headshot" loading="lazy">
                  {% endif %}
                </div>
                <div class="name-company">
                  <div class="name">
                    {{ item.name is defined and item.name|trim|length > 0
                      ? item.name
                      : 'Lorem Dolor' }}
                  </div>
                  <div class="company">
                    {{ item.role_company is defined and item.role_company|trim|length > 0
                      ? item.role_company
                      : 'Lorem ipsum, Company' }}
                  </div>
                </div>
              </div>
              {% if item.button is defined and item.button.text is defined and item.button.text|trim|length > 0 %}
                {{ macros.atomV1ButtonHtml(
                  {
                    text: item.button.text,
                    url: item.button.url
                  },
                  'bde-basicslider__button opkey-link-text',
                  design.button,
                  'columns[' ~ loop.index0 ~ '].button.text'
                ) }}
              {% else %}
                {{ macros.atomV1ButtonHtml(
                  {
                    text: 'Learn more',
                    url: '#'
                  },
                  'bde-basicslider__button opkey-link-text',
                  design.button,
                  'columns[' ~ loop.index0 ~ '].button.text'
                ) }}
              {% endif %}
            </div>
          </div>
        </div>
      </div>
    {% endfor %}
  </div>
</div>